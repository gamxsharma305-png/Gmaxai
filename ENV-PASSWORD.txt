# Vercel → Project → Settings → Environment Variables
# Add for Production + Preview + Development

# AroLinks token (https://arolinks.com dashboard)
AROLINKS_TOKEN=your_arolinks_token_here

# Secret for 12-digit codes + access tokens (NEVER put in HTML)
KEY_SECRET=change_this_to_a_long_random_string

# Real player URL — only lives on server, never in index.html
PLAYER_URL=https://freemodels.pro/chat/32tso5mtllolgj

# Optional
SITE_URL=https://your-project.vercel.app
