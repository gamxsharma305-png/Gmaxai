Gmax Playlist Hub — Token-Protected Player (Vercel)
===================================================

WHAT CHANGED
------------
- index.html me original player URL NAHI hai
- Unlock (12-digit) success → server accessToken deta hai
- Frontend /api/player ko token bhejta hai → tabhi URL milti hai
- Offline / lock / token expire → iframe = about:blank (URL hide)

FILES TO DEPLOY
---------------
index.html
api/get-key.js
api/verify.js
api/player.js      ← NEW
api/reveal.js
vercel.json

VERCEL ENV
----------
AROLINKS_TOKEN = ...
KEY_SECRET     = long random string
PLAYER_URL     = https://freemodels.pro/chat/32tso5mtllolgj

FLOW
----
1. Get Key → AroLinks → 12-digit code
2. Verify → { until, accessToken }
3. accessToken localStorage me
4. /api/player { token, deviceId } → { url }
5. iframe.src = url
6. 36h baad / offline → blank

SECURITY NOTES
--------------
- View Source me freemodels URL nahi dikhega
- Network tab me /api/player response me URL ek baar aayega (browser ko chahiye)
- Token device-bound + expiry (HMAC signed with KEY_SECRET)
- Bina valid token ke /api/player 401 deta hai
